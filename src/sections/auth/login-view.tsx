import Stack from '@mui/material/Stack';

import LoginForm from './login-form';

const LoginView = () => (
  <Stack pt={20} spacing={2} alignItems="center" justifyContent="center">
    <LoginForm />
  </Stack>
);

export default LoginView;
